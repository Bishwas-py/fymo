<script>
  // A plain Svelte component (generated by `fymo generate component`;
  // this file is yours). Import it anywhere:
  //   import __fymo_tmpl_component_name__ from '$components/__fymo_tmpl_component_name__.svelte';
  let { title = '__fymo_tmpl_component_name__', children } = $props();
</script>

<section class="card">
  <p class="eyebrow">{title}</p>
  <div class="body">
    {#if children}
      {@render children()}
    {:else}
      <p class="hint">Pass content as children to fill this card.</p>
    {/if}
  </div>
</section>

<style>
  .body {
    margin-top: 0.5rem;
  }

  .hint {
    color: var(--muted);
    margin: 0;
  }
</style>
